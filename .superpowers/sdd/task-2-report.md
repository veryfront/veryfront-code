# Task 2 report: workflow manager claim control

## RED

- Added claim-focused tests in `src/workflow/runtime/workflow-run-control.test.ts` first.
- Initial RED command:
  `deno test --no-check --allow-all src/workflow/runtime/workflow-run-control.test.ts`
- RED result: failed because `src/workflow/runtime/workflow-run-control.ts` did not export `claimWorkflowRunControl`.

## GREEN

- Added private `claimWorkflowRunControl(...)` in `src/workflow/runtime/workflow-run-control.ts`.
- Moved manager-side claim decisions into the run-control Module:
  pending lock acquire/release, pending re-read, stalled `mgr:<managerId>` claim, durable `run-execution:<executionId>` assignment, source policy restoration, running-before-spawn ordering, and failed-before/after-claim fencing.
- Adapted `WorkflowRunManager` so it keeps polling, scheduling, active execution tracking, stats, executor lifecycle, status sync, and missing-execution cleanup.

## Evidence

- `deno test --no-check --allow-all src/workflow/runtime/workflow-run-control.test.ts` -> 2 passed, 15 steps, 0 failed.
- `deno test --no-check --allow-all src/workflow/runtime/workflow-run-control.test.ts src/workflow/worker/run-manager.test.ts` -> 3 passed, 28 steps, 0 failed.
- `deno test --no-check --allow-all src/workflow/runtime/workflow-run-control.test.ts src/workflow/executor/workflow-executor.test.ts src/workflow/worker/run-manager.test.ts src/workflow/worker/run-entrypoint.test.ts src/workflow/worker/shared.test.ts src/workflow/runtime/approval-manager.test.ts src/workflow/backends/memory.test.ts src/workflow/backends/redis/index.test.ts` -> 9 passed, 207 steps, 0 failed.
- `deno test --no-check --allow-all --parallel src/workflow` -> 62 passed, 546 steps, 0 failed.
- `deno check src/workflow/runtime/workflow-run-control.ts src/workflow/runtime/workflow-run-control.test.ts src/workflow/worker/run-manager.ts src/workflow/worker/run-manager.test.ts` -> exit 0.
- `deno fmt --check src/workflow/runtime/workflow-run-control.ts src/workflow/runtime/workflow-run-control.test.ts src/workflow/worker/run-manager.ts src/workflow/worker/run-manager.test.ts` -> checked 4 files.
- `deno lint src/workflow/runtime/workflow-run-control.ts src/workflow/runtime/workflow-run-control.test.ts src/workflow/worker/run-manager.ts src/workflow/worker/run-manager.test.ts` -> checked 4 files.
- `git diff --check` -> exit 0.

## Boundary checks

- No public API, dependency, backend, approval, shared worker, entrypoint, or lockfile changes are included.
- `deno.lock` drift generated by Deno test/check runs was removed with `apply_patch`.
- Scope is limited to claim run-control, the WorkflowRunManager adapter, claim tests, and this ignored report.

## Risks

- Approval/shared reconciliation remains in the existing modules for Task 3.
- Claim uses the stricter `mgr:<managerId>` durable owner check after stalled recovery before assigning the isolated owner. This preserves the observable manager-to-execution owner transition while reducing the race window.
