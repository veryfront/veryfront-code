[project]
name = "code-optimize"
version = "0.1.0"
description = "Hybrid RLM + Batch API code optimizer"
requires-python = ">=3.12"
dependencies = [
    "rlm>=0.1.0",
    "openai>=1.0.0",
    "requests>=2.31.0",
]
