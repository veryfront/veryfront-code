[project]
name = "rlm-optimize"
version = "0.1.0"
description = "RLM-based codebase optimizer for veryfront-renderer"
requires-python = ">=3.12"
dependencies = [
    "rlm>=0.1.0",
    "openai>=1.0.0",
]

[project.scripts]
rlm-optimize = "rlm_optimize:main"
