{
  "name": "ai",
  "description": "Add AI capabilities with an AG-UI endpoint and agent support",
  "configMerge": {
    "ai": {
      "enabled": true
    }
  },
  "tips": [
    "Set VERYFRONT_API_TOKEN and VERYFRONT_PROJECT_SLUG to use Veryfront Cloud defaults locally",
    "The AG-UI endpoint is available at /api/ag-ui",
    "Customize agents in agents/ directory",
    "Add new tools in tools/ directory"
  ]
}
