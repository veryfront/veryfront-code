{
  "name": "redis",
  "description": "Redis-backed async task queue for background processing",
  "envVars": [
    {
      "name": "REDIS_URL",
      "description": "Redis connection URL",
      "required": true,
      "sensitive": false,
      "placeholder": "redis://localhost:6379",
      "docsUrl": "https://redis.io/docs/connect/clients/"
    }
  ],
  "tips": [
    "Start Redis: docker run -p 6379:6379 redis",
    "Queue background work via an app-owned API endpoint",
    "Worker processes queued tasks in the background",
    "Supports task priorities and retries"
  ]
}
