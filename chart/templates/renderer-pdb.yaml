{{- if .Values.renderer.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: veryfront-renderer
  labels:
    app: veryfront-renderer
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: veryfront-renderer
{{- end }}
