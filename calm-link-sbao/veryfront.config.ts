import type { VeryfrontConfig } from "veryfront";

const config: VeryfrontConfig = {
  projectSlug: "calm-link-sbao-k834a0",

  // Development
  dev: {
    open: true,
  },
};

export default config;
