{
  "tasks": {
    "api": "deno run --allow-net --allow-env --allow-read src/server.ts",
    "worker": "deno run --allow-net --allow-env --allow-read src/worker.ts",
    "dev": "echo 'Run \"docker-compose up -d\" then \"deno task api\" and \"deno task worker\" in separate terminals'"
  },
  "imports": {
    "redis": "https://deno.land/x/redis@v0.32.3/mod.ts",
    "veryfront": "../../src/index.ts",
    "veryfront/ai": "../../src/ai/index.ts"
  }
}
