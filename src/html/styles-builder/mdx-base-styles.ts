/**
 * Base styles available for markdown/MDX content.
 * Projects can use `prose` class from @tailwindcss/typography for styling.
 * No default prose styling is applied - projects opt-in via their own CSS.
 */
export const MDX_BASE_STYLES = `
@plugin "@tailwindcss/typography";
`;
