/**
 * RSC Actions Module
 *
 * Helper utilities for React Server Components actions.
 * Provides utilities for handling server actions in RSC contexts.
 *
 * @example
 * ```typescript
 * import { processServerAction } from "#veryfront/rendering/rsc/actions'
 *
 * const result = await processServerAction(actionId, args)
 * ```
 *
 * @module rendering/rsc/actions
 */

// Action helpers
export * from "./helpers.ts";
