{
  "name": "_base",
  "displayName": "Base Integration Files",
  "description": "Shared files for all integrations (setup guide, status API)",
  "internal": true,
  "auth": {
    "type": "none"
  },
  "envVars": [
    {
      "name": "APP_URL",
      "description": "Base URL for OAuth callbacks (e.g., http://localhost:3000)",
      "required": false,
      "sensitive": false,
      "default": "http://localhost:3000"
    }
  ],
  "tools": []
}
