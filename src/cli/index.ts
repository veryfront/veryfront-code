/**
 * CLI Module
 *
 * Command-line interface for Veryfront framework.
 * Provides commands for development, build, generation, and utilities.
 *
 * @example
 * ```typescript
 * import { main } from "#veryfront/cli'
 *
 * // Run the CLI
 * await main()
 * ```
 *
 * @module cli
 */

// Re-export everything from the index subdirectory
export * from "./index/index.ts";
