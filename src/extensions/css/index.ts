/**
 * CSS category barrel — CSS processor and compiler contracts.
 *
 * @module extensions/css
 */

export type {
  CSSCompileOptions,
  CSSCompiler,
  CSSModuleSource,
  CSSProcessor,
  CSSStylesheetSource,
} from "./css-processor.ts";
