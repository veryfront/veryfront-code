/**
 * @deprecated Use `#veryfront/compat/path` instead.
 * This module is kept for backward compatibility only.
 */
export {
  basename,
  dirname,
  extname,
  fromFileUrl,
  isAbsolute,
  join,
  relative,
  resolve,
  sep,
} from "./path/index.ts";
