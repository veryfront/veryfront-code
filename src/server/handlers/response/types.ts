/**
 * Response Handler Types
 * Shared types for response handlers
 */

// Re-export common handler types
export type {
  Handler,
  HandlerContext,
  HandlerMetadata,
  HandlerPriority,
  HandlerResult,
  RoutePattern,
} from "../types.ts";
