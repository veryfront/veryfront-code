/**
 * Base handler abstract class
 * Provides common functionality for all handlers
 *
 * Re-exported from @veryfront/security for backward compatibility.
 * New code should import directly from @veryfront/security.
 */

export { BaseHandler } from "#veryfront/security";
