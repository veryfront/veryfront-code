/**
 * Server-local compatibility barrel for public authorization contract limits.
 * The provider-neutral extension contract owns the canonical values.
 */
export {
  RSC_ACTION_AUTHORIZATION_TERMINATION_GRACE_MS,
  RSC_ACTION_AUTHORIZATION_TIMEOUT_MS,
} from "#veryfront/extensions/auth/index.ts";
