/** Maximum wire length for a canonical opaque identifier. */
export const MAX_OPAQUE_ID_CODE_UNITS = 1_024;

/** Maximum wire length for a normalized project slug. */
export const MAX_PROJECT_SLUG_CODE_UNITS = 256;
