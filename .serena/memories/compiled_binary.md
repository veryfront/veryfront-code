# Compiled Binary

```bash
deno task build          # Build binary to bin/veryfront
deno task test:e2e:binary      # Test the binary
deno task test:e2e:binary:fresh  # Rebuild + test
```

**Details**: See `DISTRIBUTION.md` for full release process and cross-platform builds.