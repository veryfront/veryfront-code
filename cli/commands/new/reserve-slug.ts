/**
 * Re-export from shared location for backward compatibility
 * @deprecated Import from "#cli/shared/reserve-slug" instead
 */
export { isSlugAvailable, reserveProjectSlug, type ReserveResult } from "#cli/shared/reserve-slug";
