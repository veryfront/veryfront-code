import { Button } from "@/shared/ui/Button"
import { Container } from "@/shared/ui/Container"
import { Heading } from "@/shared/ui/Heading"
import { Text } from "@/shared/ui/Text"
import { CodeBlock } from "@/shared/ui/CodeBlock"
import * as Features from "@/shared/ui/Features"
import {
  Terminal,
  Zap,
  Rocket,
  RefreshCw,
  FileCheck,
  Globe,
} from "https://esm.sh/lucide-react"
import { CTA } from "@/shared/ui/CTA"
import { ProductFeatures } from "@/features/home/ui/ProductFeatures"

{/* Full-width hero section */}

<div className="relative overflow-hidden">
  <div className="absolute inset-0 bg-gradient-to-br" />
  <Container className="relative z-10 py-24 md:py-32 text-center">
    <div className="max-w-4xl mx-auto space-y-6">
      <Heading
        as="h1"
        level="1"
        className="text-4xl md:text-6xl lg:text-7xl font-bold"
      >
        Build <span className="text-primary">Agents</span> super fast
      </Heading>

      <div className="max-w-3xl mx-auto space-y-4">
        <Text level="lead" className="text-xl md:text-2xl leading-relaxed">
          Build and deploy React apps instantly directly from your terminal.
        </Text>
      </div>
    </div>
  </Container>
</div>

<div className="flex flex-col gap-16 lg:gap-20">
  <ProductFeatures />
  <CTA />
</div>
