---
context:
  meta:
    title: Components
    description: Ready-made UI components for fast page building.
---

import { ComponentsHero } from "@/features/components/ui/ComponentsHero"
import { ComponentsFiltersContainer } from "@/features/components/containers/ComponentsFiltersContainer"
import { ComponentsFilters } from "@/features/components/ui/ComponentsFilters"
import { ComponentsSectionContainer } from "@/features/components/containers/ComponentsSectionContainer"
import { CTA } from "@/shared/ui/CTA"
import { Container } from "@/shared/ui/Container"

<ComponentsHero
  title="Veryfront Components"
  subtitle="Ready-made components for building consistent UIs"
/>
<div className="flex flex-col gap-16">
  <div className="flex flex-col gap-4">
    <ComponentsFiltersContainer>
      {(props) => (
        <>
          <Container>
            <ComponentsFilters {...props} />
          </Container>
          <div>
            <ComponentsSectionContainer
              library={props.library}
              useCase={props.useCase}
              category={props.category}
              searchTerm={props.searchTerm}
            />
          </div>
        </>
      )}
    </ComponentsFiltersContainer>
  </div>

  <CTA />
</div>
