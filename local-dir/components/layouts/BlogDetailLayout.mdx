---
isLayout: true
previewProps:
  children: <page rendered here>
context:
  meta:
    title: Veryfront make a long tile here
---

import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
} from "@/shared/ui/Breadcrumb"
import {
  DocsLayout,
  DocsLayoutSidebar,
  DocsLayoutContent,
  DocsLayoutBody,
} from "@/components/layouts/DocsLayout.mdx"
import { Footer } from "@/shared/ui/Footer"
import { Header } from "@/shared/ui/Header"
import { OnPageNavigation } from "@/shared/ui/OnPageNavigation"
import { FAQ } from "@/shared/ui/FAQ"
import { usePageContext } from "@/lib/usePageContext"

export function BlogBreadcrumb() {
  const pageContext = usePageContext()
  const context = pageContext.frontmatter?.context ?? {}

  return (
    <Breadcrumb>
      <BreadcrumbItem>
        <BreadcrumbLink
          href="/"
          onClick={() => {
            window.dataLayer?.push({
              event: "custom_event",
              section: "breadcrumb",
              category: "link",
              action: "clicked",
              label: "Home",
            })
          }}
        >
          Home
        </BreadcrumbLink>
      </BreadcrumbItem>
      <BreadcrumbItem>
        <BreadcrumbLink
          href="/blog"
          onClick={() => {
            window.dataLayer?.push({
              event: "custom_event",
              section: "breadcrumb",
              category: "link",
              action: "clicked",
              label: "Blog",
            })
          }}
        >
          Blog
        </BreadcrumbLink>
      </BreadcrumbItem>
      <BreadcrumbItem isCurrent>{context.title}</BreadcrumbItem>
    </Breadcrumb>
  )
}

<Header />
<DocsLayout>
  <DocsLayoutSidebar>
    <div />
  </DocsLayoutSidebar>

  <DocsLayoutContent>
    <div className="mx-auto max-w-3xl pt-3 md:pt-5">
      <BlogBreadcrumb />
      <DocsLayoutBody>{props.children}</DocsLayoutBody>
    </div>
  </DocsLayoutContent>

  <DocsLayoutSidebar>
    <OnPageNavigation
      onClick={(anchor) => {
        window.dataLayer?.push({
          event: "custom_event",
          section: "blog",
          category: "link",
          action: "clicked",
          label: anchor.text,
        })
      }}
    />
  </DocsLayoutSidebar>
</DocsLayout>

<FAQ />
<Footer />
