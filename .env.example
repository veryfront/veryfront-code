# Veryfront Renderer - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values.

# API Configuration
VERYFRONT_API_BASE_URL=https://api.example.com

# Single Project Mode (deno task renderer)
VERYFRONT_API_TOKEN=
VERYFRONT_PROJECT_SLUG=

# Headless Mode (deno task start --headless) / Split Mode (deno task start-split)
# OAuth credentials for proxy to fetch project tokens
API_CLIENT_ID=
API_CLIENT_SECRET=
REDIS_URL=

# Binary compilation
# Set to 1 to always rebuild binary, even if source unchanged
VERYFRONT_BINARY_FRESH=1

# OpenTelemetry (optional - for distributed tracing)
# OTEL_EXPORTER_OTLP_ENDPOINT=https://your-otlp-endpoint
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic ...

# Logging (debug | info | warn | error)
LOG_LEVEL=info

# OpenAI API (for batch code simplification - scripts/batch-simplify)
# OPENAI_API_KEY=sk-...
